-- Intentionally empty.
-- Destructive demo data was moved to ../demo_seed.sql so normal migration
-- deployment can never erase production applications.
select 'demo seed skipped in migrations' as result;
